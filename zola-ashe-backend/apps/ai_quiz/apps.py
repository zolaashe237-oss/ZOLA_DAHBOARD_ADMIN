from django.apps import AppConfig


class AiQuizConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.ai_quiz"
    verbose_name = "Agent IA — Génération de quiz"
